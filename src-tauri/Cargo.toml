[package]
name = "ssh-manager"
version = "0.1.0"
description = "Cross-platform SSH profile manager with integrated terminal"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
portable-pty = "0.9"
dirs = "6"
base64 = "0.22"

[profile.release]
strip = true
lto = true
